# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Android Studio / IntelliJ
.idea/
*.iml
*.ipr
*.iws
out/
local.properties
captures/
.externalNativeBuild/
.cxx/

# macOS
.DS_Store

# 서명 키 / 자격 증명 (CLAUDE.md 7절)
*.jks
*.keystore
debug.keystore
signing.properties
keystore.properties
.env

# 캡처 산출물 — 개인정보 포함 가능 (CLAUDE.md 7절)
# 검증 스크립트(-k)나 adb pull 로 저장소에 떨어질 수 있다.
*.mp4
*.m4a

# 빌드 산출물이 저장소로 복사되는 사고 방지 (정상 경로는 build/ 안이다)
*.apk
*.aab
*.aar

# 기기 제어 스크립트 산출물 (scripts/device.sh shot|dump 의 기본 저장 위치)
/shot.png
/ui.xml

# 진단 덤프 / 로그
*.hprof
*.log

# Kotlin
.kotlin/
