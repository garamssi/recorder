# 기능명세서 (Functional Specification)

- 프로젝트: ScreenRecorder (Android 16 태블릿용 화면 녹화 앱)
- 문서 버전: 1.1 (Kinetic 디자인 전면 적용, 화면 캡처·음성 전용 녹음 추가)
- 관련 문서: CLAUDE.md (개발 규칙), DESIGN_GUIDE.md (Kinetic 디자인 가이드), prompt.md (개발 지시 프롬프트)
- 표기 규칙: 각 항목의 [결정]은 확정된 기본 동작이다. 변경 시 이 문서를 먼저 수정한 후 코드를 수정한다.

---

## 1. 화면 구성 (Screen Map)

[결정] 홈·목록·설정·휴지통은 **앱 셸** 안에서 전환한다. 가로에서는 좌측 레일, 세로에서는
상단 바 + 하단 내비게이션을 쓴다 (DESIGN_GUIDE.md 4절). 플레이어만 셸 크롬 없이 전체 화면을 쓴다.

```
앱 셸 (레일 / 하단 내비)
 ├─ 녹화 (Home)               ← 레일 탭
 │   ├─ 녹화 옵션 시트 (화질/오디오/시간 제한 빠른 선택)
 │   ├─ 영역 선택 오버레이 (부분 영역 모드일 때)
 │   ├─ 카운트다운 오버레이
 │   └─ 임시 파일 복구 다이얼로그
 ├─ 보관함 (Library)          ← 레일 탭
 │   ├─ 이름 변경 / 압축(변환) / 상세 정보 / 삭제 확인 다이얼로그
 │   └─ 휴지통 화면 (Trash)   ← 보관함 하위, 탭 강조는 보관함 유지
 ├─ 설정 (Settings)           ← 레일 하단 / 상단 바 버튼
 └─ 캡처 Speed Dial (홈·보관함 우하단 고정)
     ├─ 화면 녹화 시작/중지
     ├─ 화면 캡처 (12절)
     └─ 음성 전용 녹음 시작/중지 (13절)

플레이어 (Player)             ← 셸 크롬 없음, 전체 화면
```

---

## 2. 홈 화면 (Home)

### 2.1 구성 요소

세로 스크롤 단일 컬럼으로 아래 순서로 쌓는다 (DESIGN_GUIDE.md 4절 "홈").

| 요소 | 설명 |
|---|---|
| 헤더 | "녹화 준비 완료" + 안내 문구. 녹화 중에는 "녹화 진행 중"으로 바뀐다 |
| 녹화 모드 카드 | 카드 3장: "전체 화면", "단일 앱", "부분 영역". 아이콘 + 제목 + 한 줄 설명. 선택 시 강조 외곽선. 마지막 선택값 유지 |
| 녹화 제어 카드 | 대기 시 대형 원형 녹화 버튼. 진행 중에는 REC 표시 + 경과 시간 + 일시정지/중지 버튼 |
| 현재 설정 카드 | 해상도·프레임레이트·비트레이트·오디오·시간 제한을 뱃지로 표시. "변경" 버튼으로 녹화 옵션 시트 열림 |
| 최근 녹화 카드 | 가장 최근 녹화 3개. 탭 시 플레이어로 이동, "전체 보기"로 보관함 이동 |
| 저장 공간 카드 | 남은 저장 공간과 현재 프리셋 기준 "녹화 가능 시간 추정치" |
| 캡처 Speed Dial | 우하단 고정. 화면 녹화 / 화면 캡처 / 음성 전용 녹음 (12, 13절) |

- 앱 이름·설정 진입은 셸(레일 또는 상단 바)이 담당하고, 휴지통은 보관함 화면에서 진입한다 (1절).

### 2.2 동작 규칙

- 부분 영역 모드 선택 시: 오버레이 권한(SYSTEM_ALERT_WINDOW)이 없으면 권한 안내 -> 시스템 설정으로 이동. 권한이 있으면 녹화 시작 전에 영역 선택 오버레이(드래그로 사각형 조정, 최소 크기 320x240)를 표시하고 "확인" 후 녹화 진행.
- 저장 공간이 500MB 미만이면 녹화 시작 버튼 비활성화 + 경고 문구 표시.
- 녹화 중에는 홈 화면 진입 시 "녹화 중" 상태(경과 시간, 일시정지/중지 버튼)를 표시하고, Speed Dial의 주 액션도 "중지"로 바뀐다.

---

## 3. 카운트다운

- [결정] 카운트다운 옵션을 제공한다. 선택지: "없음, 3초, 5초, 10초". 기본값: 3초.
- 카운트다운은 전체 화면 반투명 오버레이(딤 72%)에 숫자로 표시하고, 탭하면 즉시 시작(스킵)할 수 있다.
- [결정] 카운트다운은 **시스템 오버레이 창**으로 띄운다. 플로팅 버튼으로 다른 앱 위에서 녹화를 시작하면
  앱 화면이 떠 있지 않아 앱 내부 오버레이는 보이지 않기 때문이다 (기능명세서 11.1절).
  오버레이 권한이 없을 때만 앱 화면이 대신 그린다 — 두 곳에서 동시에 그리면 숫자가 겹친다.
- 카운트다운 숫자 자체는 녹화 영상에 포함되지 않도록, 인코딩 시작 시점을 카운트다운 종료 시점으로 한다.
- 설정 위치: 설정 화면 > 녹화 > "녹화 시작 카운트다운".

---

## 4. 설정 화면 (Settings)

Material 3 설정 리스트, 섹션별 그룹핑. 모든 설정은 DataStore(Preferences)에 저장하고 즉시 반영한다.

### 4.1 녹화 (비디오)

| 항목 | 선택지 | 기본값 |
|---|---|---|
| 해상도 | 기기 최대 / 1080p / 720p | 1080p |
| 프레임레이트 | 60fps / 30fps | 60fps |
| 비트레이트 | 자동(해상도x fps 기반) / 8 / 12 / 15 / 20 Mbps | 자동 (1080p60 기준 15Mbps) |
| 코덱 | H.264(호환성) / H.265 HEVC(고효율, 기기 지원 시) | H.264 |
| 녹화 시작 카운트다운 | 없음 / 3초 / 5초 / 10초 | 3초 |
| 화면 회전 처리 | 회전 따라가기 / 시작 방향 고정 | 회전 따라가기 (5절 참조) |
| 터치 표시 | 켬 / 끔 | 끔 (아래 [결정] 참조) |

- 기기 인코더가 미지원하는 조합(예: 1080p60 미지원)은 MediaCodecInfo로 사전 조회하여 선택지에서 비활성화하고 사유를 표시한다. 몰래 다운그레이드하지 않는다 (CLAUDE.md root cause 원칙과 동일 맥락).
- [결정] 터치 표시는 **1차 범위에서 보류**한다. 안드로이드에서 화면 전역의 터치 지점을 녹화에 담으려면 시스템 설정 `Settings.System.SHOW_TOUCHES`를 켜야 하는데, 이 값의 쓰기는 `WRITE_SECURE_SETTINGS`(signature|privileged) 권한이 필요해 일반 앱이 어떤 사용자 흐름으로도 획득할 수 없다. 또한 일반 앱은 다른 앱 위에서 발생하는 터치의 좌표를 관측할 수 없어(AccessibilityService 없이는 불가) 자체 오버레이로 원을 그릴 수도 없다. 설정 화면에는 항목을 비활성 상태로 노출하고, "이 기기에서 지원되지 않음(시스템 권한 필요)"을 사유로 표시한다. 개발/루팅 환경에서는 `adb shell settings put system show_touches 1`로 우회 가능함을 문서에 남긴다. AccessibilityService 기반 구현은 복잡도·권한 부담 대비 효용이 낮아 2차 이터레이션 백로그로 둔다.

### 4.2 오디오

| 항목 | 선택지 | 기본값 |
|---|---|---|
| 오디오 소스 | 무음 / 기기 내부 오디오 / 마이크 / 내부+마이크(믹싱) | 기기 내부 오디오 |
| 마이크 입력 장치 | 자동(시스템 기본) / 내장 마이크 / 블루투스 헤드셋 / 유선(USB, 3.5mm) | 자동 |
| 마이크 볼륨 | 0~200% 슬라이더 | 100% |
| 내부 오디오 볼륨 | 0~200% 슬라이더 | 100% |

- [결정] "기기 오디오"는 AudioPlaybackCaptureConfiguration으로 캡처한다. 출력이 블루투스 이어폰 등 외부 장치로 나가고 있어도 재생 캡처는 동작한다 (캡처는 출력 장치와 무관).
- [결정] 마이크 입력 장치 선택은 AudioDeviceInfo 열거 + AudioRecord.setPreferredDevice로 구현한다. 단, 블루투스 헤드셋 마이크는 SCO/LE 링크가 열려야 입력 장치로 존재하므로 setPreferredDevice만으로는 부족하다. 블루투스를 선택한 경우 AudioManager.setCommunicationDevice(API 31+)로 통신 장치를 활성화하고 OnCommunicationDeviceChangedListener로 실제 전환을 확인한 뒤(최대 3초) AudioRecord를 시작하며, 세션 종료 시 clearCommunicationDevice()로 반드시 원상 복구한다.
- [결정] 블루투스 마이크를 선택하면 출력도 SCO/LE 통신 경로로 전환되므로 A2DP 음악 재생 음질이 떨어진다. 이는 플랫폼 제약이며 기본값이 "자동"이므로 명시적으로 선택할 때만 발생한다.
- [결정] 녹화 시작 시점에 선택 장치가 연결되어 있지 않거나 통신 경로 활성화가 실패하면 "자동"으로 폴백하고 사용자에게 토스트로 알린다. 플로팅 버튼으로 다른 앱 위에서 녹화를 시작할 수 있어 앱 내 스낵바로는 보이지 않기 때문이다. 진행 알림 문구는 경과 시간 갱신으로 곧 덮이므로 쓰지 않는다.
- 다른 앱이 캡처를 허용하지 않는 오디오(allowedCapturePolicy 제한, DRM 콘텐츠 등)는 무음으로 녹음될 수 있음을 오디오 설정 화면 하단에 고정 안내 문구로 표시한다.
- 통화 오디오는 캡처하지 않는다 (플랫폼 제한 + 프라이버시).

### 4.3 저장

| 항목 | 선택지 | 기본값 |
|---|---|---|
| 저장 위치 | 기본(내부 저장소 Movies/ScreenRecorder) / 사용자 지정 폴더(SAF) | 기본 |
| 파일명 접두어 | 텍스트 입력 (영문, 숫자, 언더스코어) | "Rec" |
| 휴지통 보관 기간 | 시스템 휴지통 사용 시 30일 (시스템 고정, 변경 불가 안내만 표시) | 30일 |

### 4.4 화면 (테마)

- [결정] 앱은 **다크 테마 전용**이다 (Kinetic 디자인, DESIGN_GUIDE.md 0절). 화면 대부분이 어두운 배경 위 영상이고
  강조 레드가 곧 "녹화 중" 신호이므로 라이트 팔레트를 두지 않는다.
- [결정] 테마 선택과 다이내믹 컬러(Material You) 설정 항목은 제공하지 않는다. 설정 화면에는 고정값 안내만 표시한다.
- 런치 윈도우 배경(`android:windowBackground`)도 같은 배경색(#09090B)으로 맞춰 첫 프레임 전 흰 화면 번쩍임을 없앤다.

### 4.5 언어

- [결정] 1차 출시 언어는 한국어이며 기본 언어도 한국어다. 문자열 리소스는 `values-ko`를 기본으로 하되, 확장 대비를 위해 `values`(영어)도 함께 작성한다.
- Android 13+의 앱별 언어 설정(per-app language, `LocaleManager` + `locales_config.xml`)을 지원하여 설정 화면에 "언어: 한국어 / English / 시스템 설정 따름" 항목을 제공한다. 기본값: 한국어.
- 하드코딩 문자열 금지. 모든 UI 텍스트는 string resource로 관리한다 (클린 코드 규칙).

### 4.6 정보

- 앱 버전, 오픈소스 라이선스(Media3 등), 개인정보 처리 방침(수집 데이터 없음, 오프라인 전용 명시).

---

## 5. 화면 회전 처리

- [결정] 기본값은 "회전 따라가기": 녹화 중 기기가 회전하면 출력 영상도 회전된 화면을 그대로 담는다.
  - 구현: MediaProjection.Callback.onCapturedContentResize / 디스플레이 리스너로 크기 변화를 감지하고 VirtualDisplay를 resize한다. 인코더 출력 해상도는 세션 내 고정이므로, 가로/세로 전환 시 GPU 렌더 단계에서 스케일링하여 레터박스(위아래 또는 좌우 검은 여백)로 처리한다. MP4는 세션 중 해상도 변경이 불가하기 때문이며, 이는 우회가 아니라 컨테이너 규격상의 제약이다.
- 옵션 "시작 방향 고정": 녹화 시작 시점의 방향(가로 또는 세로)을 기준으로 인코더 해상도를 정하고, 이후 회전이 발생해도 시작 방향 기준으로 담는다 (회전된 구간은 레터박스).
- 부분 영역 모드에서는 회전 시 영역 좌표가 무효화되므로, [결정] 부분 영역 녹화 중 회전이 감지되면 녹화를 자동 일시정지하고 "영역을 다시 지정하거나 중지하세요" 알림을 표시한다.

---

## 6. 저장 정책

### 6.1 저장 위치

- [결정] 기본 저장 위치: 기기 내부 저장소의 공용 미디어 폴더 `Movies/ScreenRecorder` (MediaStore.Video 컬렉션, RELATIVE_PATH = "Movies/ScreenRecorder").
  - 사용자 파일 관리자, 갤러리 앱에서 바로 보인다.
  - WRITE_EXTERNAL_STORAGE 권한 없이 동작한다 (자기 앱이 생성한 미디어는 자유롭게 읽기/쓰기 가능).
- 사용자 지정 폴더: 설정에서 SAF(Storage Access Framework, ACTION_OPEN_DOCUMENT_TREE)로 폴더를 선택하면 이후 녹화본은 해당 폴더에 저장한다. 단, 이 경우 시스템 휴지통을 사용할 수 없으므로 삭제는 즉시 삭제(확인 다이얼로그 2단계)로 동작함을 안내한다.
  - [결정] SAF 사용자 지정 폴더는 **2차 이터레이션으로 보류**한다. SAF로 선택한 임의 트리는 MediaStore 색인 밖이라, 라이브러리 목록·재생·이름 변경·삭제·압축·플레이어가 모두 DocumentFile 기반 별도 경로를 필요로 한다(데이터 계층 이중화). 1차 범위는 기본 저장 위치(MediaStore `Movies/ScreenRecorder`)로 고정하고, 설정 화면의 "저장 위치" 항목에는 기본값을 표시하되 "사용자 지정 폴더는 추후 지원"을 부제로 안내한다. 도메인 모델 `StorageLocation.SafFolder`는 향후 확장 대비로 유지한다.
- 녹화 진행 중에는 앱 전용 캐시 디렉터리에 임시 파일로 기록하고, 정상 종료 시 최종 위치로 이동(MediaStore insert + IS_PENDING 해제)한다. 크래시 시 다음 실행에서 임시 파일을 감지해 "복구" 또는 "삭제"를 제안한다.
- [결정] 임시 파일은 크래시 안전성을 위해 fragmented MP4로 쓰지만, **발행 시 표준 MP4로 remux**한다 (ADR-0001 개정).
  fMP4는 seek 인덱스(`sidx`/`mfra`)와 `mvhd` duration이 없어 **탐색이 불가능하고 재생시간이 0으로 표시**되기 때문이다.
  재인코딩 없이 컨테이너만 바꾸며, MediaStore 파일 디스크립터에 직접 써서 중간 파일을 만들지 않는다.
  정상 종료와 크래시 복구가 같은 발행 경로를 쓰므로 복구된 파일도 동일하게 처리된다.
  remux가 실패하면 원본 fMP4를 그대로 발행한다 — 탐색이 안 되더라도 녹화물을 잃지 않는 쪽을 택한다.
- [결정] 복구/삭제는 **한 번에 하나만** 실행한다. 진행 중에는 진행 표시를 띄우고 두 버튼을 모두 누를 수 없게 한다.
  1시간짜리 녹화는 remux + 발행에 **2~4분**이 걸리는데(실측 433MB 121초, 1.09GB 161초), 버튼을 눌러도 화면이 그대로면 사용자는 안 눌린 줄 알고 다시 누른다.
  같은 임시 파일이 동시에 여러 번 발행되면 MediaStore 에 똑같은 녹화본이 그 횟수만큼 쌓인다(실제로 433MB 사본 10개가 만들어졌다).
- [결정] **발행에 실패하면 임시 파일을 지우지 않는다.** 삭제는 발행이 성공했을 때만 한다.
  지금까지는 실패해도 지워서, 저장 공간 부족 등으로 remux 와 원본 복사가 모두 실패하면
  원본도 사본도 남지 않았다. 1시간짜리 녹화에서 가장 그럴싸한 실패 사유가 바로 저장 공간 부족이다.
  - 예외는 **재생 가능한 트랙이 없다고 확인된 파일뿐**이다. 저장할 내용이 없으므로 지운다.
  - **"읽지 못했다"를 "트랙이 없다"로 접지 않는다.** 판정에 쓰는 파서와 실제 발행에 쓰는 파서는
    관용도가 다르므로, 판정이 실패한 파일도 remux 로는 살아날 수 있다. 임시 파일은 비정상 종료로
    꼬리가 잘린 fMP4 이고 그 상황이 바로 이 앱의 소실 시나리오다. 판독 실패는 발행 실패로 다뤄
    임시 파일을 남긴다.
  - 임시 파일은 앱 전용 캐시에 두는데, 저장 공간이 부족하면 OS 가 그 캐시를 임의로 비운다.
    즉 "반드시 남긴다"가 가장 필요한 상황에서 보장되지 않는다. 저장 위치 이전은 별도 과제다.
  - 보존된 임시 파일은 다음 실행의 복구 제안에 다시 나타난다.
  - 앱이 스스로 녹화물을 지우는 자동 정리는 하지 않는다. 삭제는 항상 사용자의 명시적 선택이다.
- [결정] **삭제에 실패하면 목록에 남긴다.** 복구 실패와 다르게 다룬다.
  복구 실패를 목록에서 내리는 근거는 "녹화물이 보존되니 다음에 다시 만난다"인데, 삭제 실패는
  사용자가 버리려던 파일이 안 버려진 것이다. 목록에서 내리면 사용자는 지웠다고 믿지만 그 파일은
  다음 진입에서 다시 나타난다. 실패 안내 문구도 복구와 삭제를 구분한다.
- [결정] **복구에 실패하면 그 진입에서는 같은 파일을 다시 제안하지 않는다.** 다음 진입에서 다시 제안한다.
  임시 파일을 남기기로 한 이상, 같은 이유로 계속 실패하는 파일이 생길 수 있다. 지금 복구 다이얼로그는
  `onDismissRequest` 가 비어 있어 닫을 수 없으므로, 실패가 반복되면 사용자는 "삭제"(= 녹화물 포기)
  외에는 앱을 쓸 방법이 없어진다. 실패한 파일은 이번 진입에서 내려 두고 임시 파일은 그대로 보존한다.
  - 안내 문구는 **녹화물을 지우지 않았다는 사실**을 알린다. 다이얼로그가 사라지는 것이 소실로 보이면
    안 된다. "잠시 후 다시 시도" 같은 문구는 그 진입에 재시도 수단이 없으므로 쓰지 않는다.
  - 범위는 화면 진입 단위다. 프로세스가 살아 있어도 앱에 다시 들어오면 재제안된다.
- [결정] **버려진 미완성(IS_PENDING) 레코드는 앱 시작 시 정리한다.**
  발행 도중 프로세스가 죽으면 `Movies/ScreenRecorder` 에 `is_pending=1` 레코드와 부분 기록 파일이
  남는다. 사용자에게 보이지 않으면서 수백 MB~1GB 를 점유한다(실측: 319MB). MediaStore 가 7일 뒤
  회수하므로 영구 누수는 아니지만, 그동안 저장 공간을 먹고 발행이 반복 중단되면 누적된다.
  - 판정 기준은 **"이 프로세스가 시작되기 전에 만들어졌는가"** 다. 이 프로세스가 만든 레코드는
    발행 중이거나 이미 끝난 것이고, 그보다 앞선 미완성 레코드는 그것을 만든 프로세스가 이미 없다.
  - 폴더와 `is_pending` 값만으로 판정하면 **진행 중인 압축 결과를 지운다.** 압축(8절)은 녹화본과
    같은 폴더에 같은 방식으로 발행하고, WorkManager 잡이라 녹화 상태와 무관하게 돈다. 프로세스
    시작 시각 기준은 압축도 같은 규칙으로 덮으므로 별도 예외가 필요 없다.
  - 정리는 **복구 재발행보다 먼저** 실행한다. 순서가 반대면 재발행이 고아 레코드와 파일명이
    충돌해 `(1)` 접미어가 붙는다.
  - **이 프로세스가 만든 자리는 id 로 기억해 두고 정리에서 뺀다.** 시각 비교는 이 프로세스가
    만들지 않은 것(다른 프로세스의 잔여물)에만 쓰는 폴백이다.
  - 그 시각 기준은 **`Application.onCreate` 에서 한 번** 재고 그 값을 쓴다. 늦게 재면 그 사이
    NTP 보정으로 벽시계가 앞으로 점프했을 때 기준선이 같이 밀려 **진행 중인 발행·압축을 지운다.**
    지워진 쪽은 이미 열린 스트림에 계속 쓰고 `IS_PENDING` 해제는 0행을 갱신하며 조용히 성공하므로,
    오류 없이 결과물이 사라진다. 프로세스가 어떤 레코드를 만들기도 전에 재야 그 창이 닫힌다.
  - 정리에 실패해도 앱 동작을 막지 않는다. 이것은 필수 복구가 아니라 조기 회수다.
    한 건을 못 지워도 나머지는 계속 지우고, 전체가 실패해도 복구 제안은 그대로 띄운다.
  - **압축 결과도 녹화본과 같은 발행 경로를 쓴다.** 지금은 압축 워커가 자기 발행 코드를 따로
    갖고 있어, 스트림을 열지 못하면 복사를 건너뛰고도 `IS_PENDING` 을 해제해 **0바이트 파일을
    성공으로 발행**하고(6절 위반), 복사가 실패하면 미완성 레코드를 정리하지 않는다.
    같은 경로를 쓰면 이 두 결함이 구조적으로 사라지고, 압축 결과도 "이 프로세스가 만든 자리"
    로 기억돼 시각 폴백에 기대지 않는다.
  - 회수한 건수와 대상을 로그에 남긴다. 사용자에게는 알리지 않는다 — 보이지 않던 것을 앞당겨
    치우는 것뿐이다. 다만 잘못 지웠을 때 진단할 수단은 있어야 한다.
    **이 로그는 release 빌드에서도 남아야 한다.** 파일을 지우는 코드의 유일한 진단 수단인데
    R8 규칙이 걷어 가면, 그것을 감싼 catch 는 4절이 금지한 빈 catch 가 된다.
- [결정] **녹화본의 코덱과 fps 는 실제 트랙에서 읽는다.** 지금은 코덱이 H264 로 하드코딩돼 있어
  HEVC 로 녹화해도 라이브러리 상세에 H264 로 보이고, fps 는 카메라 전용 메타데이터 키를 읽어
  사실상 0 이라 화면에 아예 표시되지 않는다. 둘 다 사용자에게 틀린 정보를 보여 준다.
  - 트랙의 MIME 으로 코덱을, 트랙 포맷의 프레임레이트로 fps 를 정한다. 복구 발행은 세션 설정을
    모르므로 파일 자체에서 읽어야 두 경로가 같은 값을 낸다.
- [결정] **복구 발행도 정상 발행과 같은 보호를 받는다.** 한번 시작하면 화면을 벗어나도 끝까지 간다.
  정상 발행은 코디네이터의 앱 수명 스코프에서 `NonCancellable` 로 감싸 돌지만, 복구 발행은
  화면의 `viewModelScope` 에서 돌아 **사용자가 홈을 벗어나면 remux 도중에 취소된다.** 발행은
  2~4분 걸리므로 그 사이 화면을 떠나는 것은 드문 일이 아니고, 취소되면 아무 안내 없이 실패한다.
  - 삭제도 같다. 중간에 취소되면 지웠는지 아닌지 알 수 없는 상태가 된다.
  - 프로세스가 죽는 것까지는 막지 못한다. 그것은 임시 파일 보존과 고아 정리로 받아 낸다.
- [결정] **발행 중임을 알림과 플로팅 버블에도 보여 준다.** 홈 화면은 이미 "저장 중..."을 표시한다.
  1시간짜리 녹화의 발행은 2~4분이 걸리는데, 그 사이 알림은 "녹화 중 00:57:08" 문구 그대로 남고
  버블은 유휴 메뉴를 보여 준다. 사용자는 다른 앱을 쓰는 중이라 그 둘만 보이므로 깜깜하다.
  - 발행 중에는 중지·일시정지·새 녹화 시작을 제공하지 않는다. 특히 **버블은 이 구간에 "녹화 시작"을
    노출하지 않는다** — 노출하면 MediaProjection 동의만 소비하고 서비스가 START 를 조용히 무시한다.
  - 세 진입점(홈·알림·버블)은 같은 뜻의 문구를 쓴다.
- [결정] **완료 알림은 발행이 끝난 시점에 띄운다.** 자동 중지 시점이 아니다.
  지금은 자동 중지 이벤트에서 바로 띄워, 발행이 시작도 되기 전에 "녹화 완료" 가 뜬다. 발행 중
  상태를 표시하기 시작한 이상 **"녹화 완료" 와 "저장하는 중" 이 같은 순간에 나란히 뜬다.**
  그리고 발행 도중 프로세스가 죽으면 **없는 파일에 대한 완료 알림만 남는다.**
  - 자동 중지 사유(시간 제한 도달 등)는 그때까지 들고 있다가 완료 알림 문구에 쓴다.
  - **수동 중지도 완료를 알린다.** 사용자는 다른 앱을 쓰는 중이라 "저장하는 중" 이 조용히
    사라지는 것이 전부인데, 그러면 정상 종료와 프로세스 사망이 구별되지 않는다.
  - 저장할 내용이 없어 발행이 아무것도 남기지 않은 세션은 완료를 알리지 않는다.
- [결정] **누를 수 있어 보이는데 아무 일도 일어나지 않는 조작을 두지 않는다.** 상태마다 실제로
  동작하는 것만 남긴다. 이 규칙은 발행 중(`Stopping`)뿐 아니라 카운트다운 중에도 같이 적용한다.
  - 카운트다운 중 알림은 **중지만** 둔다. 일시정지는 이 구간에서 아무 일도 하지 않는다
    (세션이 아직 없어 코디네이터가 즉시 반환한다). 중지는 카운트다운을 취소하므로 남긴다.
  - 카운트다운 중 플로팅 버블도 유휴 메뉴를 보여 주지 않는다. `Stopping` 과 같은 이유다 —
    "녹화 시작" 을 누르면 MediaProjection 동의만 소비하고 아무 일도 일어나지 않는다.
- [결정] **이미 진행 중인 세션 때문에 새 녹화 요청을 버릴 때는 그 사실을 알리고, 소비된 동의 토큰을 비운다.**
  지금은 조용히 버린다. 사용자는 동의까지 하고 아무 일도 안 일어나는 것을 본다. 화면 캡처·음성
  녹음은 같은 상황에서 이미 안내하고 있으므로(`capture_busy_recording`) 화면 녹화만 다를 이유가 없다.
  - 버려진 요청의 토큰을 그대로 두면 쓰이지 않은 동의가 메모리에 남는다. 7절("토큰은 메모리에서만
    유지하고 세션마다 새로 받는다")에 어긋나므로 버릴 때 함께 비운다.

### 6.2 파일명 규칙

- [결정] 기본 파일명: `{접두어}_{YYYYMMDD}_{HHmmss}.{확장자}`
  - 예: `Rec_20260819_143025.mp4`
  - 접두어 기본값 "Rec", 설정에서 변경 가능 (4.3절).
  - 동일 초에 충돌 시 `_1`, `_2` 순번을 붙인다. 순번은 같은 확장자 안에서만 매긴다.
  - 확장자: 화면 녹화 `.mp4`, 화면 캡처 `.png` (12절), 음성 전용 녹음 `.m4a` (13절).
- [결정] 녹화 종료 직후 "저장됨" 스낵바/다이얼로그에서 즉시 이름을 입력해 바꿀 수 있다 (입력하지 않으면 기본 이름 유지).

### 6.3 이름 변경 (Rename)

- [결정] 저장된 녹화본의 이름 변경을 지원한다.
  - 진입점: 녹화 목록의 항목 메뉴 > "이름 변경", 플레이어 화면 메뉴 > "이름 변경".
  - 구현: MediaStore의 DISPLAY_NAME 업데이트. 자기 앱이 생성한 파일이므로 별도 권한 불필요.
  - 유효성: 빈 이름 금지, 파일명 금지 문자(`/ \ : * ? " < > |`) 입력 차단, 최대 100자, 확장자 `.mp4`는 고정 표시(수정 불가).
  - 중복 이름이면 저장 시 순번 자동 부여 여부를 물어본다.

---

## 7. 녹화 목록 화면 (Library)

### 7.1 표현 방식

- 화면 상단에 제목 + 부제, 우측에 "선택" 버튼과 휴지통 진입 버튼을 둔다. 그 아래 검색 필드 · 정렬 메뉴 · 레이아웃 토글 툴바를 둔다.
- [결정] 기본 레이아웃: 리스트형(행 단위). 각 행 구성:
  - 좌: 썸네일(16:9, 영상 첫 프레임 아님 - 1초 지점 프레임 사용, 하단에 재생 시간 뱃지 오버레이)
  - 중: 파일명(1줄, 말줄임), 보조 정보 1줄 "2026-08-19 14:30, 1080p 60fps, 128MB"
  - 우: 더보기(점 3개) 메뉴
- 우상단 토글로 그리드형(2~4열, 화면 폭 반응형) 전환 가능. 선택값 유지.
- 정렬: 최신순(기본) / 오래된순 / 이름순 / 크기순(큰 파일 우선).
- 검색: 상단 검색바로 파일명 부분 일치 검색.
- 빈 상태: 아이콘 + "아직 녹화한 영상이 없습니다". 검색 결과가 없을 때는 "검색과 일치하는 녹화본이 없습니다"를 표시한다.
- 썸네일은 로컬 캐시(파일 URI + 수정시각 키)로 관리하여 스크롤 성능을 확보한다.

### 7.2 항목 액션 (더보기 메뉴)

1. 재생 (플레이어 화면으로 이동, 항목 탭과 동일)
2. 이름 변경
3. 공유 (ACTION_SEND, content:// URI + FLAG_GRANT_READ_URI_PERMISSION)
4. 압축 (8절 참조)
5. 상세 정보 (파일명, 경로, 해상도, fps, 코덱, 비트레이트, 길이, 크기, 생성일시)
6. 삭제 (휴지통으로 이동, 7.3절)

### 7.3 삭제 및 다중 선택

- 항목 길게 누르기 또는 상단 "선택" 버튼 -> 다중 선택 모드 진입. 상단에 선택 개수 / 전체 선택 / 공유 / 삭제 / 닫기를 둔다.
- [결정] 선택 모드에서 선택되지 않은 항목은 흐리게(50%) 표시하고, 선택된 항목은 강조 외곽선 + 체크 아이콘으로 구분한다.
- [결정] 마지막 항목을 해제해도 선택 모드는 유지한다. 종료는 닫기(X) 버튼으로만 한다 (실수로 모드가 풀리지 않게).
- [결정] 삭제는 항상 2단계다: (1) 확인 다이얼로그 "N개 항목을 휴지통으로 이동할까요? 30일 후 자동 삭제됩니다" -> (2) 휴지통 이동.
- 스와이프 삭제는 제공하지 않는다 (오조작으로 인한 녹화물 손실 방지).

---

## 8. 압축 (변환)

- 전제: 녹화 자체가 이미 H.264/HEVC로 압축 인코딩되므로 "압축"은 재인코딩(트랜스코딩)을 의미한다. 이 사실을 압축 다이얼로그 상단에 명시한다 ("재인코딩으로 용량을 줄입니다. 화질이 일부 낮아질 수 있습니다").
- [결정] 압축 프리셋 3종:

| 프리셋 | 처리 | 예상 효과 |
|---|---|---|
| 고효율 변환 | 동일 해상도, HEVC(H.265) 재인코딩 (기기 지원 시) | 화질 유사, 용량 약 30~50% 감소 |
| 표준 압축 | 동일 해상도, 비트레이트 50%로 재인코딩 | 용량 약 50% 감소, 화질 저하 소폭 |
| 최대 압축 | 720p 다운스케일 + 비트레이트 축소 | 용량 약 70% 이상 감소, 화질 저하 |

- [결정] 원본은 삭제하지 않고 `원본이름_compressed.mp4`로 새 파일을 생성한다. 완료 후 "원본을 휴지통으로 이동할까요?"를 물어본다.
- 구현: Media3 Transformer(권장) 또는 MediaCodec 디코드-인코드 파이프라인. WorkManager 백그라운드 작업 + 진행률 알림, 취소 가능.
- 녹화 중에는 압축 작업을 시작할 수 없다 (인코더 자원 경합 방지, 사유를 토스트로 안내).

---

## 9. 휴지통 (Trash)

- [결정] 기본 저장 위치(MediaStore) 사용 시 Android 11+의 시스템 휴지통을 사용한다.
  - 삭제 = `MediaStore.createTrashRequest`로 IS_TRASHED = 1 처리. 시스템이 30일 후 자동 영구 삭제한다 (보관 기간은 시스템 정책으로 고정).
  - 물리적 위치는 시스템이 관리하는 숨김 영역이며 사용자가 직접 접근할 필요가 없다. 앱 내 "휴지통 화면"이 유일한 진입점이다.
- 휴지통 화면 (보관함 상단 휴지통 아이콘으로 진입):
  - 그리드 카드로 표시한다. [결정] 삭제된 항목임이 한눈에 보이도록 **썸네일은 채도 0(흑백)**, 제목에는 취소선을 적용하고 남은 보관일("N일 후 삭제")을 강조색으로 표시한다.
  - 카드를 탭해 다중 선택한다. 선택이 있으면 상단에 "복원" / "영구 삭제"(확인 다이얼로그 필수), 선택이 없으면 "휴지통 비우기"를 표시한다.
- SAF 사용자 지정 폴더 사용 시에는 시스템 휴지통을 적용할 수 없으므로, 삭제는 즉시 영구 삭제이며 확인 다이얼로그에 이를 명시한다 (자체 휴지통 폴더 구현은 1차 범위에서 제외 - 복잡도 대비 효용 낮음, 필요 시 2차 이터레이션).

---

## 10. 플레이어 화면 (Player)

- [결정] Media3 ExoPlayer 기반 앱 내장 플레이어를 구현한다. 외부 플레이어 위임이 아니라 자체 화면이다.
- 기능:
  - 재생/일시정지, 시크바(썸네일 프리뷰는 2차 범위), 앞으로/뒤로 10초 점프
  - 배속: 0.5x~2.0x를 0.1 단위로 선택 (드롭다운). 기본 1.0x
  - [결정] 컨트롤은 ExoPlayer 내장 컨트롤러 대신 Compose로 직접 그린다 (DESIGN_GUIDE.md 4절 "플레이어").
    상단(뒤로/제목/더보기), 중앙(-10초 · 재생·일시정지 · +10초), 하단(위치·시크바·길이 · 배속 · 전체화면) 3단 구성이다.
  - [결정] 재생 중 3초간 조작이 없으면 컨트롤을 서서히 감추고, 화면을 탭하면 즉시 다시 표시한다.
  - [결정] **화면 채우기** 토글 — 플레이어는 이미 화면을 꽉 채우므로 "전체 화면"은 영상을 확대·크롭해 레터박스를 없애는 모드를 뜻한다(`RESIZE_MODE_ZOOM` ↔ `RESIZE_MODE_FIT`). 동시에 시스템 바를 감추며, 뒤로가기로도 해제된다. 회전 시 재생 위치를 유지한다.
  - [결정] 끝까지 재생된 뒤 재생 버튼을 누르면 **처음부터 다시 재생**한다. 이때 버튼 아이콘도 "처음부터 재생"으로 바뀐다 (ExoPlayer는 종료 상태에서 play()만으로는 아무 일도 일어나지 않는다).
  - [결정] **볼륨 컨트롤** — 하단 컨트롤에 음소거 토글 + 볼륨 슬라이더를 둔다.
    시스템 미디어 볼륨(STREAM_MUSIC)에 연동해 하드웨어 볼륨 키와 항상 같은 값을 가리킨다.
    플레이어 자체 볼륨(ExoPlayer.volume)을 따로 두면 하드웨어 키와 어긋나 "소리가 안 난다"는 혼란을 부른다.
    볼륨이 0이거나 음소거면 아이콘이 음소거 상태로 바뀐다. 슬라이더를 올리면 음소거가 자동 해제된다.
    AudioManager 접근은 data 계층의 인터페이스 뒤로 격리한다 (CLAUDE.md 3절).
    볼륨 컨트롤은 하단 두 번째 줄 왼쪽(배속·화면 채우기는 오른쪽)에 하나의 pill로 묶어 배치한다.
  - [결정] 음소거 상태는 Settings에 기록되지 않아 시스템 변경 알림이 오지 않는다. 앱이 직접 볼륨·음소거를
    바꾼 뒤에는 저장소가 스스로 다시 읽어 UI에 반영한다(하드웨어 키 변경은 Settings 관찰로 반영된다).
  - 상단 메뉴: 이름 변경, 공유, 상세 정보, 삭제(휴지통 이동, 삭제 시 목록으로 복귀)
  - 화면 꺼짐 방지 (재생 중 KEEP_SCREEN_ON)
- 재생 목록 개념은 두지 않는다. 단일 파일 재생 후 목록으로 복귀 (1차 범위).

---

## 11. 녹화 중 UX 및 녹화 제어

### 11.1 공통 UX

- Foreground Service 알림: 경과 시간(초 단위 갱신), 일시정지/재개, 중지 버튼.
- [결정] **플로팅 캡처 버튼**은 앱 화면 안의 FAB가 아니라 다른 앱 위에 떠 있는 오버레이 창이다. 태블릿 홈이나 다른 앱을 쓰는 중에도 캡처를 시작할 수 있어야 하기 때문이다.
  - 설정 > 녹화 > "플로팅 캡처 버튼" (기본 끔). 켤 때 "다른 앱 위에 표시"(SYSTEM_ALERT_WINDOW) 권한이 없으면 시스템 설정으로 보낸다.
  - 접힘 상태는 원형 "+" 버튼이고, 탭하면 화면 녹화 / 화면 캡처 / 음성만 녹음 / **시간 제한** / **앱으로 가기** 메뉴가 펼쳐진다 (시간 제한은 11.4절).
  - [결정] "앱으로 가기"는 런처 인텐트로 앱 화면을 연다. 다른 앱을 쓰다가 목록·설정으로 돌아올 경로가 필요하기 때문이다.
  - [결정] 드래그로 옮길 수 있고, 손을 떼면 가까운 **좌우 가장자리에 스냅**한다 (세로 위치는 자유). 화면 중앙에 남기면 콘텐츠를 계속 가리기 때문이다.
  - 가장자리에 붙은 채 펼치면 창이 넓어지므로, 붙어 있던 변 기준으로 위치를 다시 계산해 화면 밖으로 나가지 않게 한다.
  - 녹화·녹음 중에는 경과 시간과 일시정지/중지를 담은 pill로 바뀌며, 시간 영역을 탭하면 "앱으로 가기"가 펼쳐진다.
    화면 캡처·음성 녹음은 세션이 겹쳐 시작할 수 없으므로 진행 중 메뉴에서 제외한다.
  - [결정] 펼치고 접어도 **기준 요소(접힘 "+" 버튼, 녹화 중 pill)는 세로로도 가로로도 움직이지 않는다.**
    사용자가 놓아둔 자리가 기준이며, 메뉴는 기준 요소의 **위로** 펼치는 것이 기본이고 위쪽 공간이
    모자라면 **아래로** 펼친다. 버튼이 펼칠 때마다 따라 움직이면 다음에 누를 자리를 매번 다시
    찾아야 하기 때문이다.
  - [결정] 붙어 있는 변에 따라 메뉴 줄의 **라벨 위치와 정렬을 뒤집는다.** 오른쪽에 붙으면 라벨이 버튼 왼쪽,
    왼쪽에 붙으면 버튼 오른쪽이다. 항상 한쪽으로 정렬하면 펼칠 때 창이 라벨 너비만큼 넓어지면서
    기준 요소가 가로로 밀린다.
  - [결정] 펼침 메뉴는 화면 밖으로 나가지 않는다. 위·아래 어느 쪽으로도 담지 못할 때만 창 전체를 화면 안으로
    밀어 넣으며, 이때 밀린 양은 기준 위치에 반영하지 않는다(접으면 원래 자리로 돌아온다).
    시스템 바 인셋을 피해 배치한다(상태 바에 겹치면 터치가 SystemUI로 가서 버블이 눌리지 않는다).
  - 버블을 띄워 두려면 포그라운드 서비스가 필요하다(Android 8+ 백그라운드 실행 제한). `specialUse` 타입 서비스와 상시 알림을 쓰고, 알림에 "숨기기" 액션을 둔다.
  - [결정] 버블에서 화면 녹화·캡처를 시작할 때는 투명 트램폴린 액티비티로 MediaProjection 동의를 받는다. 사용자가 보던 화면이 뒤로 그대로 보인다.
  - [결정] 부분 영역 모드는 영역 선택 오버레이가 필요하므로 버블에서 시작하면 앱을 열어 정상 플로를 태운다. 몰래 전체 화면으로 대체하지 않는다.
  - 버블 자체가 녹화·캡처 결과물에 찍히는 것은 오버레이 특성상 불가피하므로 설정 설명에 명시한다.
- [결정] 앱 화면(홈·보관함)에는 캡처 FAB를 두지 않는다. 앱 안에서의 녹화 진입점은 홈의 대형 녹화 버튼이다.
- 중지 경로: 알림 버튼, 플로팅 버블, 시스템 상태 바 녹화 칩, 홈 화면의 중지 버튼. 어느 경로로 중지해도 파일이 안전하게 마무리되어야 한다.
- 저장 공간이 녹화 중 200MB 이하로 떨어지면 자동으로 안전 중지하고 사유를 알림으로 표시한다.

### 11.2 녹화 일시정지 (Pause)

- 진입점: 알림의 "일시정지" 버튼, 플로팅 버블, 홈 화면의 녹화 중 상태 카드.
- [결정] 동작 방식:
  - 일시정지 즉시 비디오 프레임과 오디오 샘플을 인코더에 공급하지 않는다. 하나의 MP4 세션은 유지한다 (파일을 나누지 않는다).
  - 일시정지 구간은 최종 영상에 포함되지 않는다. 재개 시 presentationTimeUs를 일시정지 누적 시간만큼 보정하여 영상이 끊김 없이 이어지도록 한다. 비디오와 오디오 트랙의 보정량은 동일해야 하며, 재개 지점의 A/V 동기 오차는 1프레임(약 16.7ms) 이내여야 한다 (계측 테스트로 검증).
  - MediaProjection 세션과 사용자 동의는 유지되므로 재개 시 시스템 동의 다이얼로그가 다시 뜨지 않는다.
- 상태 표시: 알림과 플로팅 버블의 경과 시간이 멈추고 "일시정지됨" 문구 표시. 시스템 녹화 칩은 시스템 동작을 따른다.
- 일시정지 상태 제한:
  - 일시정지 중에도 Foreground Service와 화면 캡처 자원은 유지된다 (배터리 소모가 있음을 설정 도움말에 명시).
  - [결정] 일시정지 상태로 30분이 지나면 자동으로 안전 중지하고 파일을 저장한다 (자원 점유, 실수로 방치 방지). 중지 5분 전 알림으로 예고한다.
  - 일시정지 중 화면 회전, 부분 영역 무효화 등 세션 조건이 깨지는 이벤트가 발생하면 5절의 규칙을 동일하게 적용한다.

### 11.3 녹화 재개 (Resume)

- 진입점: 일시정지와 동일한 세 경로의 "재개" 버튼.
- [결정] 재개 시 카운트다운은 적용하지 않고 즉시 재개한다 (카운트다운은 최초 시작에만 적용).
- 재개 직후 첫 프레임은 키프레임(sync frame)을 강제 요청하여 재개 지점의 화질 깨짐을 방지한다.
- 일시정지-재개는 한 녹화 세션에서 횟수 제한 없이 가능하다.
- 참고: "재시작(처음부터 다시 녹화)" 기능은 별도로 두지 않는다. 동일 목적은 "중지 후 새 녹화 시작"으로 달성 가능하며, 진행분 폐기 여부를 묻는 UX가 오조작으로 인한 녹화물 손실 위험만 키우기 때문이다. 필요 시 백로그로 재검토한다.

### 11.4 타이머 녹화 (사용자가 정한 시간만큼 녹화)

- 개요: 사용자가 지정한 시간이 경과하면 녹화를 자동으로 안전 중지하고 저장한다.
- 진입점: 홈 화면의 녹화 옵션 시트 내 "녹화 시간 제한" 항목, 그리고 플로팅 캡처 버블의 "시간 제한" 메뉴.
- [결정] 플로팅 버블에도 같은 진입점을 둔다. 다른 앱을 쓰는 중에 시간 제한을 걸려면 앱으로 돌아가 옵션 시트를 여는 두 단계를 거쳐야 해서, 버블로 녹화를 시작하는 흐름과 어긋나기 때문이다.
  - 버블 메뉴 줄에는 현재 값을 함께 보여준다 (예: "시간 제한 · 10:00", 없으면 "시간 제한 · 제한 없음").
  - 탭하면 시/분/초 직접 입력 창이 뜬다.
- [결정] 두 진입점은 **같은 입력 창**을 쓴다. 제목·안내 문구·증감 버튼 모양·버튼 구성(제한 없음 / 취소 / 저장)이 모두 같고, 카드 안의 모든 줄을 가운데 한 축에 맞춘다. 같은 값을 고치는 같은 창인데 홈과 버블이 달라 보이면 사용자가 서로 다른 기능으로 읽는다.
  - 플로팅 버블은 오버레이 창이라 플랫폼 뷰로, 홈은 Compose로 그린다. 구현 수단은 다르지만 문구·간격·버튼 구성은 같은 값을 쓴다.
  - "제한 없음" 버튼은 두 창 모두에 둔다. 옵션 시트의 "제한 없음" 칩과 겹치지만, 창만 열어도 해제할 수 있어야 버블에서와 조작이 같아진다.
  - 두 진입점은 같은 설정([AppSettings.recording.timeLimit])을 읽고 쓴다. 한쪽에서 바꾸면 다른 쪽에 즉시 반영된다 — 버블 전용 값을 따로 두지 않는다.
- [결정] 입력 창은 **바깥을 눌러서 닫지 않는다.** "제한 없음", "취소", "저장" 세 버튼으로만 닫는다 (뒤로 가기는 취소와 같다).
  - 증감 버튼은 작고 위아래로 틈이 있어 빠르게 여러 번 누르면 손끝이 그 틈으로 조금씩 벗어난다. 바깥 탭을 닫기로 쓰면 값을 고치던 중에 창이 사라져 설정 자체가 되지 않는다.
  - 키보드가 올라오면 카드가 위로 밀린다. 방금 누른 자리를 다시 눌러도 카드 바깥이 되므로, 좌표로 정확히 갈라내도 오조작이 남는다. 닫는 길을 버튼으로 좁히는 편이 확실하다.
  - 두 진입점 모두 같다 — 홈의 Compose 창도 바깥 탭으로 닫지 않는다.
- [결정] 선택지: "제한 없음(기본) / 직접 입력". 고정 시간 프리셋(30초·1분·…·1시간)은 제공하지 않는다 — 사용자가 필요한 시간을 직접 입력하는 편이 명확하고 옵션 시트가 간결해진다.
  - 직접 입력: 시/분/초 단위 입력, 최소 10초, 최대 12시간. 유효 범위를 벗어나면 입력 단계에서 차단하고 사유를 표시한다. 확정하면 그 값이 시간 제한으로 적용된다.
  - [결정] 각 칸 위아래에 1씩 올리고 내리는 **증감 버튼**을 둔다. 키보드만으로 넣게 하면 값을 조금 고치는 데도 키보드를 띄워 지우고 다시 쳐야 하고, 버블 오버레이에서는 그 부담이 특히 크다. 키보드 입력은 그대로 함께 쓸 수 있다.
    - 증감 범위는 칸별로 시 0~12, 분 0~59, 초 0~59다. **자리 넘김은 하지 않는다** — 59분에서 올려도 시가 늘지 않는다. 한 번 눌러 총 시간이 크게 뛰면 무엇이 바뀌었는지 알기 어렵기 때문이다.
    - [결정] 분·초 칸은 끝에 닿으면 **그 칸 안에서 순환한다** (0에서 내리면 59, 59에서 올리면 0). 옆 칸은 건드리지 않으므로 자리 넘김이 아니다. 끝에서 멈추면 버튼을 눌러도 아무 일이 없어 고장으로 보이고, 0분에서 59분으로 가려면 ▲를 59번 눌러야 한다.
    - [결정] 시 칸은 순환하지 않고 0과 12에서 멈춘다. 한 번 눌러 12시간이 뛰면 총합이 통째로 달라져 무엇이 바뀌었는지 알기 어렵다. 분·초의 순환은 최대 59초를 움직이지만 시는 그렇지 않다.
    - 순환은 증감 버튼에만 적용한다. 키보드로 99를 쳐 넣으면 여전히 59로 맞춰 담는다 — 친 값을 다른 값으로 뒤집는 것과 한 눈금 옮기는 것은 다른 조작이다.
    - 칸별 범위와 총합 검증(10초~12시간)은 별개다. 예를 들어 12시 30분은 칸별로는 넣을 수 있지만 총합이 12시간을 넘어 저장이 막힌다.
    - 길게 누르면 연속으로 증감한다. 12시간처럼 큰 값을 한 번에 올리기 위해서다.
    - 홈 옵션 시트와 플로팅 버블 두 진입점 모두 같은 규칙을 쓴다.
  - 마지막 선택값을 유지하되, "제한 없음"이 아닌 값은 홈 화면 프리셋 요약 카드에 표시한다 (예: "1080p, 60fps, 내부 오디오, 10분 제한").
- 동작 규칙:
  - 시간 기준은 실제 녹화 시간이다. 카운트다운은 포함하지 않으며, 일시정지 구간도 포함하지 않는다 (일시정지하면 타이머도 함께 멈추고 재개 시 이어서 계산한다).
  - 남은 시간을 알림과 플로팅 버블에 경과 시간과 병기하여 표시한다 (예: "03:24 / 10:00").
  - 종료 1분 전과 10초 전에 알림을 갱신하여 예고한다 (1분 이하 설정 시 10초 전 예고만).
  - 시간 도달 시 자동 안전 중지 -> 저장 -> "녹화가 완료되었습니다" 알림 (탭 시 플레이어로 이동).
  - 자동 중지 전에 사용자가 수동으로 중지하면 그 시점까지 저장한다.
  - 녹화 중 시간 제한을 해제하거나 연장하는 기능은 1차 범위에서 제외한다 (백로그).
  - 구현 시 타이머는 시스템 시계 변경에 영향받지 않도록 elapsedRealtime 기준으로 계산한다.

---

## 12. 화면 캡처 (Screenshot)

캡처 Speed Dial의 "화면 캡처" 액션으로 현재 화면을 이미지 한 장으로 저장한다.

- [결정] 저장 위치는 `Pictures/ScreenRecorder`, 형식은 PNG다. 파일명 규칙은 6.2절과 같고 확장자만 `.png`다.
- [결정] Android 14+ 규칙에 따라 캡처 1장에도 매번 MediaProjection 동의를 새로 받는다. 토큰은 메모리에만 두고 1회 사용 후 폐기한다 (CLAUDE.md 7절).
- 동작: 동의 → mediaProjection 타입 포그라운드 서비스 기동 → VirtualDisplay + ImageReader로 첫 프레임 수신 → PNG 저장 → 서비스 종료.
- [결정] 동의 직후 0.5초를 기다린 뒤 캡처한다. 첫 프레임에는 사라지는 중인 시스템 동의 다이얼로그가 찍히는데,
  사용자가 원한 것은 그 아래 화면이기 때문이다.
- [결정] 프레임을 3초 안에 받지 못하면 실패로 처리하고 알림으로 안내한다. 화면을 몰래 다시 시도하지 않는다.
- [결정] 화면 녹화나 음성 녹음이 진행 중이면 거부하고 "다른 캡처가 이미 진행 중입니다"를 알린다 (MediaProjection 세션은 동시에 하나만 열 수 있다).
- 결과는 알림으로 안내한다. 캡처 이미지는 녹화 목록(보관함)에 표시하지 않는다 — 보관함은 영상 전용이다.

## 13. 음성 전용 녹음 (Voice Only)

캡처 Speed Dial의 "음성만 녹음" 액션으로 화면 없이 마이크만 녹음한다.

- [결정] 저장 위치는 `Music/ScreenRecorder`, 형식은 AAC-LC를 담은 `.m4a`다. 파일명 규칙은 6.2절과 같다.
- [결정] 화면을 캡처하지 않으므로 MediaProjection 동의는 받지 않는다. microphone 타입 포그라운드 서비스만 쓴다.
- 마이크 입력 장치와 마이크 볼륨은 4.2절의 설정을 그대로 따른다.
- [결정] 4.2절의 폴백 안내도 그대로 적용한다. 선택한 마이크를 쓸 수 없으면 시작 직후 토스트로 알린다.
- 진행 알림에 경과 시간을 1초 간격으로 표시하고, Speed Dial의 해당 항목은 "중지"로 바뀐다.
- [결정] 화면 녹화 중에는 시작을 거부한다 (마이크 입력이 겹친다). 이미 녹음 중일 때의 중복 시작도 거부한다.
- [결정] 녹음된 내용이 없으면(시작 직후 중지 등) 임시 파일만 정리하고 "녹음된 내용이 없습니다"를 알린다. 빈 파일을 저장하지 않는다.
- 일시정지는 제공하지 않는다 (짧은 메모 용도).

---

## 14. 1차 출시 범위 제외 (Backlog)

다음은 이번 범위에서 제외하며, 코어 완성 후 별도 이터레이션으로 진행한다:

- 트리밍 편집기, GIF 내보내기
- 전면 카메라 PIP 오버레이
- 스타일러스 주석 오버레이
- 마이크/내부 오디오 분리 트랙 저장
- 장시간 녹화 파일 자동 분할
- 시크바 썸네일 프리뷰
- SAF 폴더용 자체 휴지통
- 녹화 중 시간 제한 해제/연장, "처음부터 재시작" 기능

---

## 15. 요구사항 추적표 (요약)

| ID | 요구사항 | 관련 절 |
|---|---|---|
| FR-01 | 전체 화면 녹화 (FHD, 60fps 목표) | 2, prompt.md |
| FR-02 | 부분 녹화 (단일 앱 / 사용자 지정 영역) | 2, prompt.md |
| FR-03 | 백그라운드 녹화 + 내부/마이크 오디오 | 4.2, 11 |
| FR-04 | 카운트다운 (없음/3/5/10초, 기본 3초) | 3 |
| FR-05 | 저장 위치: Movies/ScreenRecorder + SAF 옵션 | 6.1 |
| FR-06 | 기본 파일명 Rec_YYYYMMDD_HHmmss.mp4, 접두어 변경 | 6.2 |
| FR-07 | 이름 변경 (저장 직후 + 목록 + 플레이어) | 6.3 |
| FR-08 | 녹화 목록 (리스트/그리드, 정렬, 검색, 다중 선택) | 7 |
| FR-09 | 삭제 = 휴지통 이동 (30일), 복원/영구 삭제 | 7.3, 9 |
| FR-10 | 압축(트랜스코딩) 프리셋 3종, 원본 보존 | 8 |
| FR-11 | 테마: 다크 고정 (Kinetic) | 4.4 |
| FR-12 | 언어: 한국어 기본, 앱별 언어 설정 | 4.5 |
| FR-13 | 마이크 입력 장치 선택 (내장/블루투스/USB) | 4.2 |
| FR-14 | 회전: 따라가기(기본) / 시작 방향 고정 | 5 |
| FR-15 | 내장 플레이어 (ExoPlayer, 배속, 시크) | 10 |
| FR-16 | 터치 표시, 플로팅 컨트롤 옵션 | 4.1, 11.1 |
| FR-17 | 녹화 일시정지 (구간 미포함, A/V 동기 보정) | 11.2 |
| FR-18 | 녹화 재개 (즉시 재개, 키프레임 강제) | 11.3 |
| FR-19 | 타이머 녹화 (프리셋 + 직접 입력, 자동 안전 중지) | 11.4 |
| FR-20 | 화면 캡처 1장 저장 (PNG, Pictures/ScreenRecorder) | 12 |
| FR-21 | 음성 전용 녹음 (m4a, Music/ScreenRecorder) | 13 |
| FR-22 | 앱 셸 내비게이션 (가로 레일 / 세로 하단 바) + 캡처 Speed Dial | 1 |
